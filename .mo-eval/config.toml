language = "go"
# No path here: the task appends the exact test to this command.
test_command = "go test"
repo = "go-resty/resty"   # this is a fork; its merged pull requests are upstream's
# Keep in step with the setup-go version in .github/workflows/mo-eval.yml.
worker_image = "golang:1.27-bookworm@sha256:69a7b9788769bec032d238959b61854e9ae87f57be9029ec04e9885fabf99195"
worker_platform = "linux/amd64"
